-- a script that displays the number of records with id = 89 in the table first_table.
-- the database will be passed as argument of the mysql command.
SELECT COUNT(*) FROM first_table WHERE id = 89;
